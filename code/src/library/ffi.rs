// SPDX-License-Identifier: GPL-3.0-or-later

/// An example function which uses an FFI call.
pub fn call_from_ffi_safe() { unsafe { ffi_local::call_from_ffi() }; }

/// ## FFI Includes
///
/// This module contains bindings generated by `bindgen` during the build phase. It is a
/// non-public module so we can "wrap" the `unsafe` FFI calls in safe wrappers.
mod ffi_local
{
	include!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/ffi/build/bindings.rs"));
}
